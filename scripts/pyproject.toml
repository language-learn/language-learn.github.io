[project]
name = "deutsch-c1-anki"
version = "0.1.0"
description = "Anki deck generator for German C1 learning platform"
requires-python = ">=3.8"
dependencies = [
    "genanki>=0.13.0",
]

[project.scripts]
create-anki-deck = "create_anki_deck:main"
